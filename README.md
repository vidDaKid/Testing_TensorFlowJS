This is a fully functional digit recognition ML model.
It gets the saved model from tfjs/saved_model/*
Use `node new_backend/new_server.js` to start the backend
Use `cd tfjs && npm start` to start the frontend
~ V
